server:
  port: 8081
  servlet:
    context-path: /api²

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mystore
    username: root
    password: 1999

  jpa:
    show_sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
